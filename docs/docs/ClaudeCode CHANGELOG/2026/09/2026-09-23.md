## Claude によるアップデート（2026-09-23）

フロントエンドをAWS（S3 + CloudFront）にデプロイするための準備として、APIの接続先を環境変数化しました。ユーザーからの明示的な依頼により、通常は本人が行うコード修正を今回はClaude Codeが代行しています。

### 実施した内容

1. **APIベースURLの環境変数化**
   `RebuildReact/.env.development`（`VITE_API_BASE_URL=http://localhost:8080`）と
   `RebuildReact/.env.production`（`VITE_API_BASE_URL=http://<EC2のパブリックIP>:8080`）を新規作成。

2. **ハードコードされていた `http://localhost:8080` の置き換え**
   以下8ファイル・26箇所すべてを `${import.meta.env.VITE_API_BASE_URL}` を使う形に修正しました。
   ダブルクォート/シングルクォートで書かれていた箇所は、テンプレートリテラル（バッククォート）に変更しています。
   - `src/components/layout/Header/Header.tsx`
   - `src/pages/RegisterPage/RegisterPage.tsx`
   - `src/pages/LoginPage/LoginPage.tsx`
   - `src/pages/HomePage/HomePage.tsx`
   - `src/pages/MemoPage/MemoPage.tsx`
   - `src/pages/StoryCreatePage/StoryCreatePage.tsx`
   - `src/pages/CreateMangaPage/TopicChoice.tsx`
   - `src/pages/CreateMangaPage/CreateMangaPage.tsx`

3. **動作確認**
   `npm run lint`（エラー0件）、`npm run test`（5件成功）、`npm run build` が通ることを確認。
   ビルド後の `dist/` 内のJSに、本番用IPアドレスが正しく埋め込まれていることも確認済み。

これにより、`npm run dev`（ローカル開発）は従来通り `localhost:8080` を、`npm run build`（本番ビルド）はEC2上のバックエンドを参照するようになります。
