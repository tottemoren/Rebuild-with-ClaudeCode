## Claude によるアップデート（2026-09-24）

フロントエンドのCD（継続的デプロイ）自動化に着手。GitHub ActionsからAWS（S3・CloudFront）へのデプロイを試みる過程で、OIDC認証の設定が原因不明のまま失敗し、一旦アクセスキー方式に切り替えることになった。その経緯と、中途半端な状態で残っているものを記録しておく。

### 実施した内容

1. **`.github/workflows/frontend-deploy.yml` を新規作成**（ユーザー作成）
   push時に `RebuildReact` の lint/test/build を行い、S3へアップロード後CloudFrontのキャッシュを削除するワークフロー。
   認証方式は当初OIDCを想定していたが、後述の理由により現時点では未完成（AWS認証ステップが失敗する状態）。

2. **OIDC認証を試みたが未解決のまま中断**
   - IAM ID プロバイダー（`token.actions.githubusercontent.com`）、IAMロール（`github-actions-rebuild-frontend`）、インラインポリシー（`rebuild-frontend-deploy-policy`、S3の該当バケットとCloudFrontの該当ディストリビューションに限定した最小権限）を作成
   - 信頼ポリシー・プロバイダー設定・ワークフローファイル・GitHubリポジトリのWorkflow permissions設定をすべて確認したが、いずれも設定上の誤りは見当たらなかった
   - **プロバイダー・ロールを一度削除して作り直しても同じ `Not authorized to perform sts:AssumeRoleWithWebIdentity` エラーが再発**し、原因を特定できていない
   - CloudTrailでの詳細調査も試みたが、コンソールのUIが想定と異なり完了できず

3. **方針変更: アクセスキー方式に切り替え**
   時間対効果を考え、原因究明は一旦保留し、IAMユーザー + アクセスキーによるシンプルな認証方式でCD自体を先に完成させる方針に変更。

### 現時点で中途半端に残っているもの（要フォローアップ）

- IAM ID プロバイダー `token.actions.githubusercontent.com`（動作未確認のまま）
- IAMロール `github-actions-rebuild-frontend`（インラインポリシー `rebuild-frontend-deploy-policy` 付き。動作未確認のまま）
- `.github/workflows/frontend-deploy.yml` の「AWSの認証情報を取得（OIDC）」ステップは、このロールARNを参照したままなので、**アクセスキー方式に置き換える修正が別途必要**
- OIDCの原因調査自体は、後日あらためて挑戦する想定（上記のIAMリソースは、再挑戦時にそのまま使えるよう、今は削除せず残す方針）
